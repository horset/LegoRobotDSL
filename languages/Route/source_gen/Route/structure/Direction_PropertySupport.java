package Route.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class Direction_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<Direction> constants = ListSequence.fromList(Direction.getConstants()).iterator();
    while (constants.hasNext()) {
      Direction constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }
  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<Direction> constants = ListSequence.fromList(Direction.getConstants()).iterator();
    while (constants.hasNext()) {
      Direction constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }
  public String fromInternalValue(String value) {
    Direction constant = Direction.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
